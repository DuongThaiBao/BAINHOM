#term 
Repository(repo)
branch name
conflict
local
remote
#comment
-git init
-get status
-get add
-git reset
-git commit
-git merge {brach name}
- git checkout -b {branch name}
-git brach -d {branch name}